// Git Reflex — Installable PWA + service worker (app shell + active deck)
// Issue #95 · frontend · feature · milestone: Phase 2 · Grow
// Design: docs/superpowers/specs/2026-06-16-git-reflex-refined-design.md
// TODO(#95): implement Installable PWA + service worker (app shell + active deck)

import * as React from "react";

export interface InstallablePwaServiceWorkerProps {
  // TODO(#95): props
}

export function InstallablePwaServiceWorker(_props: InstallablePwaServiceWorkerProps) {
  // structure-first scaffold for #95
  return null;
}

export default InstallablePwaServiceWorker;
