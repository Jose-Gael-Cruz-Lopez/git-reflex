// Tests — #96 Offline review queue (IndexedDB) + replay on reconnect
import { describe, it, expect } from "vitest";

describe("#96 offline-review-queue-replay-on-reconnect", () => {
  it.todo("implements: Offline review queue (IndexedDB) + replay on reconnect");
  it("scaffold present", () => { expect(true).toBe(true); });
});
