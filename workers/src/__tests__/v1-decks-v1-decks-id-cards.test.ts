// Tests — #61 /v1/decks + /v1/decks/:id/cards (KV edge cache)
import { describe, it, expect } from "vitest";

describe("#61 v1-decks-v1-decks-id-cards", () => {
  it.todo("implements: /v1/decks + /v1/decks/:id/cards (KV edge cache)");
  it("scaffold present", () => { expect(true).toBe(true); });
});
