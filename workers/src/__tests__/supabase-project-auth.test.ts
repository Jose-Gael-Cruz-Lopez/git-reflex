// Tests — #46 Supabase project + Auth (magic link + GitHub/Google OAuth)
import { describe, it, expect } from "vitest";

describe("#46 supabase-project-auth", () => {
  it.todo("implements: Supabase project + Auth (magic link + GitHub/Google OAuth)");
  it("scaffold present", () => { expect(true).toBe(true); });
});
