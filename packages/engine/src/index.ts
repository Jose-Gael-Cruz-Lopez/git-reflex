// @git-reflex/engine — barrel.
// The git execution engine lives at the core (real git in the browser via WASM).
// Modules are scaffolded per Phase 0 issues and wired in here.

export const ENGINE_VERSION = "0.0.0-scaffold";
