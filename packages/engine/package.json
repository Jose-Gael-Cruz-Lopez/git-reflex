{
  "name": "@git-reflex/engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Shared in-browser git engine (WASM) — the architectural keystone.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  }
}
